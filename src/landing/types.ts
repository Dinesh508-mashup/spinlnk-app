export interface HostelData {
  name: string;
  owner: string;
  area: string;
  city: string;
  machines: number;
  rooms: number;
  contact: string;
  lat: number;
  lng: number;
}

export interface ModalData {
  name: string;
  owner: string;
  location: string;
  contact: string;
}
